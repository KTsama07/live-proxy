{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "Production CORS proxy for HLS streams with in-memory caching",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.3.1"
  }
}